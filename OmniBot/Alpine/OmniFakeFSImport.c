// Compile the upstream fakefs archive importer into OmniBot without copying or
// modifying its implementation. The ish-arm64 source remains pinned as a Git
// submodule, which also preserves its GPL provenance.
#include "tools/fakefs.c"
